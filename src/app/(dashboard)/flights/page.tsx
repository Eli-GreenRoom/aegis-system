import EmptyModule from "@/components/dashboard/EmptyModule";

export default function FlightsPage() {
  return (
    <EmptyModule
      title="Flights"
      hint="Arrival and departure legs per person. Drop a confirmation, AI fills the form."
    />
  );
}
