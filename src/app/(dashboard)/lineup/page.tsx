import EmptyModule from "@/components/dashboard/EmptyModule";

export default function LineupPage() {
  return (
    <EmptyModule
      title="Lineup"
      hint="Stages, slots, and sets. Drag to reorder, batch-tag for announce."
    />
  );
}
