import EmptyModule from "@/components/dashboard/EmptyModule";

export default function GroundPage() {
  return (
    <EmptyModule
      title="Ground"
      hint="Pickups, dropoffs, vehicles, vendors. Airport ↔ hotel ↔ stage."
    />
  );
}
