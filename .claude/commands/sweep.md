---
description: Run the full pre-push sweep — non-ASCII, Suspense, tsc, tests.
---

Run `npm run check`. If it fails, list each failure with file:line
and propose fixes as concrete diffs. Do not push until green.
